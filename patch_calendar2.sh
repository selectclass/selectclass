sed -i 's/return eventNames;/cotacoes.forEach(c => { if (!c.date) return; const cDate = new Date(c.date); let isWithinRange = false; if (c.endDate) { const end = new Date(c.endDate); let current = new Date(cDate); while (current <= end) { if (current.toDateString() === dayDateString) { isWithinRange = true; break; } current.setDate(current.getDate() + 1); } } else { if (cDate.toDateString() === dayDateString) { isWithinRange = true; } } if (isWithinRange) { const name = `Cotação: ${c.title}`; if (!eventNames.includes(name)) { eventNames.push(name); } } }); return eventNames;/g' components/Calendar.tsx
